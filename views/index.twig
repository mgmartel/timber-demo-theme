{% extends "base.twig" %}

{% block content %}
	<h2>{{foo}}</h2>
	<p>{{qux}}</p>
	<p>{{options.timber_example_field}}</p>
	{% for post in posts %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	{% endfor %}

{% endblock %}